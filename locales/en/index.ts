
import { common } from './common';
import { ui } from './ui';
import { flow } from './flow';
import { plugins } from './plugins';
import { server } from './server';

export const en = {
  ...common,
  ...ui,
  ...flow,
  ...plugins,
  ...server
};
